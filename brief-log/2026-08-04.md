# Build Log — 2026-08-04 (Session 4)

## What did I try today?

- Ran the full fork → clone → branch → draft-PR workflow for the first time: created my fork of the course repo, pushed a one-emoji smoke-test PR ([#9](https://github.com/clg236/applied-generative-ai-course-students/pull/9)) to prove the pipeline end-to-end, and the professor's validation workflow passed it.
- Started Phase P2 of this team project (branch `sanja`): built the complete validation & cleaning module — declarative YAML rule engine, standardizers (dates, units, enums), missing-data derivation, quality scoring, `validation_report.json`, and the stage CLI with `--no-llm` / `--limit` / `--log-json` — plus 76 offline unit tests and a 50k-record streaming check (flat memory).

## What surprised me or failed?

- Git auto-configured my commit author as `sanjakrstic@…wireless.nyu.edu` — my NYU username and wireless hostname would have been **public forever** in the fork. My agent flagged it before the push; I fixed the identity to the GitHub no-reply address. Lesson: check `git config` before the first commit, not after.
- A config bug I only caught by hand-checking fixture output: the severity synonym `urgent-safety` mapped to `MAJOR` because the shorter synonym `urgent` matched first. Fixed by prioritizing longer synonyms and SAFETY-first table order. Rules-as-data is nice, but the data itself needs adversarial review.
- The frozen P2 schema can't represent a record with a missing required provenance field, yet the phase rules say never drop a record. Resolution: flag it (`missing_provenance_*`), emit it anyway with a `model_validation_failed` marker, and stash dropped raw values as `raw_<field>` extras so nothing is lost.

## What will I do differently next?

- Set `user.name` / `user.email` globally before touching any repo.
- Review config-driven mappings (synonyms, thresholds, severities) with adversarial examples before trusting them.
- When a schema can't represent a value, stash the raw value instead of dropping it — and say so in `fixes_applied`.

## AI use

- Tools/models: Hermes agent (OpenRouter, `deepseek/deepseek-v4-flash`) for implementation and review; Claude API path exists in the module but was not live-tested (no key in the environment, tests run fully offline).
- Important prompts: the Phase P2 kickoff ("read `PRODUCT_DEVELOPMENT_MANAGER.md`, implement only Sanja's tasks on branch `sanja`, never merge") and repeated review prompts against the frozen schema and CLI contract.
- Where the output helped: module scaffold, rule engine, test suite — and catching the author-email leak before push.
- Where my judgment changed or rejected it: I hand-verified all 24 expected fixture lines and rejected the first mapping results (severity bug above); I rejected "drop the value" as a fix and insisted on `raw_<field>` stashing; I rejected double-reporting `duration_mismatch` on top of `end_before_start`.
