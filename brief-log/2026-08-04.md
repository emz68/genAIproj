# Build Log — 2026-08-04 (Session 4)

Team reflection for the EV Charger Data Platform (genAIproj), per `assignments/build-logs/brief.md` (three prompts + AI use; small and real).

**Sources:** this log consolidates what is verifiable from the repository. Where a member's own notes are not available, the log records only what `main` history and the merge review show — no invented reflection. Personal next-steps and AI use are documented in full for Sanja; for the other members those fields say "not documented here" and they are welcome to add their own.

**Work link:** https://github.com/emz68/genAIproj — all five phases were merged to `main` on 2026-08-10 by the Integration Manager, and the merged pipeline ran the official NYC DOT dataset end-to-end (258,343 raw records → 247,886 golden records, offline; outputs in `docs/demo/`).

---

## Emlyn — P1 Data & Ingestion

- **Tried:** format readers (CSV incl. malformed/BOM, GeoJSON, free text), LLM maintenance-event extraction with `--no-llm` fallback, the messiness injector (`--seed`/`--scale`), fixtures + tests. (Commits `8b034cf`, `ed4466a`, `5386340`)
- **Surprised / failed:** the merge review found a §6-protocol deviation in P1: the final metrics/error JSON line must be unconditional — fixed by the Integration Manager in `d33f684`. Cleanup commits also show dead code and duplicated address/location extraction rules that needed deduplication (`ed4466a`, `5386340`).
- **Will do differently:** not documented here (cleanup commits suggest: verify protocol conformance before hand-off; keep extraction rules deduplicated).
- **AI use:** not documented here.

## Sanja — P2 Validation & Cleaning

- **Tried:** first fork → clone → branch → draft-PR run on the course repo (one-emoji smoke PR, passed the professor's validation workflow); the full validation & cleaning module — declarative YAML rule engine, standardizers (dates, units, enums), missing-data derivation, quality scoring, `validation_report.json`, stage CLI (`--no-llm`/`--limit`/`--log-json`) — with 76 offline unit tests and a 50k-record streaming check. (Commits `fc9784d`, `29facb5`, `1518a78`)
- **Surprised / failed:** (1) Git auto-configured my commit author as `sanjakrstic@…wireless.nyu.edu` — NYU username + wireless hostname, public forever; caught before push, fixed to the GitHub no-reply address. (2) Synonym bug: `urgent-safety` mapped to `MAJOR` because the shorter synonym `urgent` matched first — fixed by prioritizing longer synonyms and SAFETY-first table order. (3) The frozen P2 schema can't represent a record with a missing required provenance field; solved by flagging + emitting with a `model_validation_failed` marker and stashing dropped values as `raw_<field>` extras. (4) Post-merge review found two §6-protocol deviations in my `run.py`: the final metrics line must be unconditional, and corrupt JSONL must hard-fail instead of being skipped — fixed by the Integration Manager in `640ee4f`.
- **Will do differently:** set `user.name`/`user.email` globally before the first commit; review config-driven mappings adversarially; when a schema can't represent a value, stash the raw value instead of dropping it; re-check the §6 stderr protocol details before claiming done.
- **AI use:** Hermes agent (OpenRouter, `deepseek/deepseek-v4-flash`) for implementation and review; Claude API path exists in the module but was not live-tested (no key in the environment; tests run fully offline). Important prompts: the P2 kickoff ("read `PRODUCT_DEVELOPMENT_MANAGER.md`, implement only Sanja's tasks on branch `sanja`, never merge") and repeated review prompts against the frozen schema/CLI contract. Output helped with the module scaffold, rule engine and test suite, and caught the author-email leak. Where judgment rejected it: I hand-verified all 24 expected fixture lines and rejected the first mapping results (severity bug); rejected "drop the value" as a fix (insisted on `raw_<field>` stashing); rejected double-reporting `duration_mismatch` on top of `end_before_start`.

## Yash — P3 Reconciliation & Anomaly Detection

- **Tried:** entity resolution, golden records, anomaly detection (duplicate billing, outliers, utilization cliffs, concurrent sessions), safety/health metrics (`est_uptime_pct` etc.), fixtures + precision/recall tests. (Commit `386ce5b`)
- **Surprised / failed:** not documented here; P3 merged cleanly with no visible post-merge fixes (`37c045a`).
- **Will do differently:** not documented here.
- **AI use:** not documented here.

## Emily — P4 Reporting & Experience

- **Tried:** PSC compliance report (CSV + HTML with lineage appendix), operational dashboard, safety & infrastructure-health view, customer-experience view (M1–M5). (Commit `95acd7a`)
- **Surprised / failed:** not documented here; P4 merged cleanly with no visible post-merge fixes (`00b662e`).
- **Will do differently:** not documented here.
- **AI use:** not documented here.

## Anastasia — P5 Platform & Integration Manager

- **Tried:** orchestrator with resumability/manifest invalidation, config & secrets handling, pipeline-health monitoring, integration harness, merge-readiness checker, stubs (`031eb1d`, `46581c5`); then, as Integration Manager: unioned module requirements into root (§8.3, `268ff65`), merged all five branches, triaged the §8.6 punch list (protocol fixes for P1 `d33f684` and P2 `640ee4f`), integrated the official NYC DOT dataset (Con Edison territory) and built the presentation demo package (`48ed213`, `24b2d6c`, `920c157`).
- **Surprised / failed:** the merge review found §6 stderr-protocol deviations in P1 and P2 (final metrics/error line not unconditional; corrupt-JSONL handling) — the concrete failures the punch list fixed.
- **Will do differently:** not documented here.
- **AI use:** not documented here.
